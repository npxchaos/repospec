---
'@repospec/protocol': patch
'@repospec/engine': patch
'@repospec/cli': patch
'@repospec/templates': patch
---

Refresh package READMEs to the current, feature-complete surface: all nine CLI
commands, six adapters, the `LlmClient` port, the plugin runtime, and the
manifest/lockfile schemas (they previously described a Milestone 1–3 subset).
