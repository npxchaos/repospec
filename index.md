---
layout: home

hero:
  name: Repospec
  text: The repository is the source of truth — not the AI.
  tagline: >-
    An open, repository-first specification that standardizes how AI coding
    assistants understand, build, and evolve a project — without prompts.
  actions:
    - theme: brand
      text: Why Repospec
      link: /docs/vision
    - theme: alt
      text: Command reference
      link: /docs/commands
    - theme: alt
      text: The Specification
      link: /spec/README

features:
  - title: One source, every assistant
    details: >-
      Author a single .repospec/ directory. Adapters project it into AGENTS.md,
      CLAUDE.md, Cursor, Copilot, Windsurf, Gemini, Zed, Cline, and Continue —
      plus native Claude Code subagents — kept in sync with one command.
  - title: A standard, not a tool
    details: >-
      The Specification is language-neutral and versioned independently of the
      reference CLI. If the CLI vanished, any tool built against the spec would
      still stand.
  - title: Owned by humans, generated by machine
    details: >-
      You edit .repospec/; Repospec regenerates the tool entrypoints. Generated
      files carry a managed checksum and are never clobbered when you hand-edit
      them.
  - title: Opt-in, sandboxed plugins
    details: >-
      Community plugins run only after explicit, integrity-pinned consent — in a
      subprocess under Node's Permission Model with no filesystem access and
      capability-gated network.
---

## Quickstart

```bash
# run without installing
npx @repospec/cli init

# validate and keep entrypoints in sync
repospec doctor
repospec sync
repospec sync --check   # CI: fail if entrypoints drift from .repospec/
```

`repospec init` writes the human-owned `.repospec/` source of truth and the
Repospec-owned tool entrypoints. Edit `.repospec/`, then run `repospec sync` to
update every assistant's file at once.

Start with **[Why Repospec](/docs/vision)**, keep the
**[command reference](/docs/commands)** handy, and read the
**[Specification](/spec/README)** to implement it in any language.
